import { CheckboxValueDirectiveDirective } from './checkbox-value-directive.directive';

describe('CheckboxValueDirectiveDirective', () => {
  it('should create an instance', () => {
    const directive = new CheckboxValueDirectiveDirective();
    expect(directive).toBeTruthy();
  });
});
